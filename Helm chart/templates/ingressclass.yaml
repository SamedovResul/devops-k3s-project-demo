{{- if .Values.ingressClass.createIngressClass }}
apiVersion: networking.k8s.io/v1
kind: IngressClass
metadata:
  name: {{ .Values.ingressClass.name }}
spec:
  controller: nginx.org/ingress-controller
{{- end }}