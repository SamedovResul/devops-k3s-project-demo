apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mongo
spec:
  selector:
    app: {{ .Release.Name }}-mongo
  ports:
  - name: mongo
    port: {{ .Values.mongodb.port }}
    targetPort: {{ .Values.mongodb.port }}
