apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mqtt-external
spec:
  type: NodePort
  selector:
    app: {{ .Release.Name }}-mqtt
  ports:
    - name: mqtt
      port: 1883
      targetPort: 1883
      nodePort: 31883
